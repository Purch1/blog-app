export enum PostType {
  POST = 'post',
  PAGE = 'page',
  STORY = 'story',
  SERIES = 'series',
}

export enum StatusType {
  DRAFT = 'draft',
  SCHEDULED = 'schedule',
  REVIEW = 'review',
  PUBLISH = 'published',
}
